---
title: CLI
description: Opzioni e comandi della CLI di Darkie Coder.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

La CLI di Darkie Coder, per impostazione predefinita, avvia la [TUI](/docs/tui) quando viene eseguita senza argomenti.

```bash
darkie-coder
```

Ma accetta anche i comandi documentati in questa pagina. Questo ti permette di interagire con Darkie Coder in modo programmatico.

```bash
darkie-coder run "Explain how closures work in JavaScript"
```

---

### tui

Avvia l'interfaccia testuale di Darkie Coder (interfaccia utente da terminale).

```bash
darkie-coder [project]
```

#### Flag

| Flag                                     | Breve | Descrizione                                                              |
| ---------------------------------------- | ----- | ------------------------------------------------------------------------ |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continua l'ultima sessione                                               |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID sessione da continuare                                                |
| <nobr><code>{"--fork"}</code></nobr>     |       | Duplica la sessione quando continui (usa con `--continue` o `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |       | Prompt da usare                                                          |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modello nel formato provider/model                                       |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente da usare                                                          |
| <nobr><code>{"--port"}</code></nobr>     |       | Porta su cui mettersi in ascolto                                         |
| <nobr><code>{"--hostname"}</code></nobr> |       | Hostname su cui mettersi in ascolto                                      |

---

## Comandi

La CLI di Darkie Coder include anche i seguenti comandi.

---

### agent

Gestisci gli agenti per Darkie Coder.

```bash
darkie-coder agent [command]
```

---

### attach

Collega un terminale a un backend server di Darkie Coder già in esecuzione avviato tramite i comandi `serve` o `web`.

```bash
darkie-coder attach [url]
```

Questo consente di usare la TUI con un backend Darkie Coder remoto. Per esempio:

```bash
# Start the backend server for web/mobile access
darkie-coder web --port 4096 --hostname 0.0.0.0

# In un altro terminale, collega la TUI al backend in esecuzione
darkie-coder attach http://10.20.30.40:4096
```

#### Flag

| Flag                                     | Breve | Descrizione                                                                                     |
| ---------------------------------------- | ----- | ----------------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Working directory in cui avviare la TUI                                                         |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continua l'ultima sessione                                                                      |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID sessione da continuare                                                                       |
| <nobr><code>{"--fork"}</code></nobr>     |       | Crea un fork della sessione durante la continuazione (usa con `--continue` o `--session`)       |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Password per l'autenticazione di base (predefinita: `DARKIE_CODER_SERVER_PASSWORD`)                 |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Nome utente per l'autenticazione di base (predefinito: `DARKIE_CODER_SERVER_USERNAME` o `darkie-coder`) |

---

#### create

Crea un nuovo agente con configurazione personalizzata.

```bash
darkie-coder agent create
```

Questo comando ti guida nella creazione di un nuovo agente con un system prompt personalizzato e configurazione degli strumenti.

---

#### list

Elenca tutti gli agenti disponibili.

```bash
darkie-coder agent list
```

---

### auth

Comando per gestire le credenziali e il login dei provider.

```bash
darkie-coder auth [command]
```

---

#### login

Darkie Coder si basa sull'elenco provider di [Models.dev](https://models.dev), quindi puoi usare `darkie-coder auth login` per configurare le API key per qualunque provider tu voglia usare. Le credenziali vengono salvate in `~/.local/share/darkie-coder/auth.json`.

```bash
darkie-coder auth login
```

Quando Darkie Coder si avvia, carica i provider dal file delle credenziali e, se presenti, anche eventuali key definite nell'ambiente o in un file `.env` nel progetto.

---

#### list

Elenca tutti i provider autenticati come salvati nel file delle credenziali.

```bash
darkie-coder auth list
```

Oppure la versione corta.

```bash
darkie-coder auth ls
```

---

#### logout

Esegue il logout da un provider rimuovendolo dal file delle credenziali.

```bash
darkie-coder auth logout
```

---

### github

Gestisci l'agente GitHub per l'automazione dei repository.

```bash
darkie-coder github [command]
```

---

#### install

Installa l'agente GitHub nel tuo repository.

```bash
darkie-coder github install
```

Questo configura il workflow GitHub Actions necessario e ti guida nel processo di configurazione. [Scopri di più](/docs/github).

---

#### run

Esegui l'agente GitHub. Tipicamente usato in GitHub Actions.

```bash
darkie-coder github run
```

##### Flag

| Flag                                  | Descrizione                                  |
| ------------------------------------- | -------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | Evento GitHub mock per cui eseguire l'agente |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token                 |

---

### mcp

Gestisci i server Model Context Protocol.

```bash
darkie-coder mcp [command]
```

---

#### add

Aggiungi un server MCP alla tua configurazione.

```bash
darkie-coder mcp add
```

Questo comando ti guida nell'aggiunta di un server MCP locale o remoto.

---

#### list

Elenca tutti i server MCP configurati e il loro stato di connessione.

```bash
darkie-coder mcp list
```

Oppure la versione corta.

```bash
darkie-coder mcp ls
```

---

#### auth

Autentica con un server MCP con OAuth abilitato.

```bash
darkie-coder mcp auth [name]
```

Se non fornisci un nome server, ti verrà chiesto di selezionare tra i server OAuth-capable disponibili.

Puoi anche elencare i server OAuth-capable e il loro stato di autenticazione.

```bash
darkie-coder mcp auth list
```

Oppure la versione corta.

```bash
darkie-coder mcp auth ls
```

---

#### logout

Rimuovi le credenziali OAuth per un server MCP.

```bash
darkie-coder mcp logout [name]
```

---

#### debug

Esegui debug di problemi di connessione OAuth per un server MCP.

```bash
darkie-coder mcp debug <name>
```

---

### models

Elenca tutti i modelli disponibili dai provider configurati.

```bash
darkie-coder models [provider]
```

Questo comando mostra tutti i modelli disponibili tra i provider configurati nel formato `provider/model`.

È utile per capire l'esatto nome del modello da usare nella [config](/docs/config/).

Puoi anche passare opzionalmente un ID provider per filtrare i modelli a quel provider.

```bash
darkie-coder models anthropic
```

#### Flag

| Flag                                    | Descrizione                                        |
| --------------------------------------- | -------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Aggiorna la cache modelli da models.dev            |
| <nobr><code>{"--verbose"}</code></nobr> | Output più verboso (include metadati come i costi) |

Usa `--refresh` per aggiornare l'elenco modelli in cache. È utile quando nuovi modelli vengono aggiunti a un provider e vuoi vederli in Darkie Coder.

```bash
darkie-coder models --refresh
```

---

### run

Esegui darkie-coder in modalità non interattiva passando un prompt direttamente.

```bash
darkie-coder run [message..]
```

È utile per scripting, automazione o quando vuoi una risposta rapida senza avviare la TUI completa. Per esempio:

```bash "darkie-coder run"
darkie-coder run Explain the use of context in Go
```

Puoi anche collegarti a una istanza `darkie-coder serve` già in esecuzione per evitare i cold boot dei server MCP ad ogni esecuzione:

```bash
# Avvia un server headless in un terminale
darkie-coder serve

# In un altro terminale, esegui comandi che si collegano ad esso
darkie-coder run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flag

| Flag                                     | Breve | Descrizione                                                                                     |
| ---------------------------------------- | ----- | ----------------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |       | Il comando da eseguire; usa message per gli argomenti                                           |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continua l'ultima sessione                                                                      |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID sessione da continuare                                                                       |
| <nobr><code>{"--fork"}</code></nobr>     |       | Duplica la sessione quando continui (usa con `--continue` o `--session`)                        |
| <nobr><code>{"--share"}</code></nobr>    |       | Condividi la sessione                                                                           |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modello nel formato provider/model                                                              |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente da usare                                                                                 |
| <nobr><code>{"--file"}</code></nobr>     | `-f`  | File da allegare al messaggio                                                                   |
| <nobr><code>{"--format"}</code></nobr>   |       | Formato: default (formattato) o json (eventi JSON grezzi)                                       |
| <nobr><code>{"--title"}</code></nobr>    |       | Titolo sessione (usa prompt troncato se non viene fornito un valore)                            |
| <nobr><code>{"--attach"}</code></nobr>   |       | Attach a un server darkie-coder in esecuzione (es. http://localhost:4096)                           |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Password per l'autenticazione di base (predefinita: `DARKIE_CODER_SERVER_PASSWORD`)                 |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Nome utente per l'autenticazione di base (predefinito: `DARKIE_CODER_SERVER_USERNAME` o `darkie-coder`) |
| <nobr><code>{"--dir"}</code></nobr>      |       | Directory di esecuzione, o percorso sul server remoto durante il collegamento                   |
| <nobr><code>{"--variant"}</code></nobr>  |       | Variante del modello (sforzo di ragionamento specifico del provider)                            |
| <nobr><code>{"--thinking"}</code></nobr> |       | Mostra blocchi di pensiero                                                                      |
| <nobr><code>{"--port"}</code></nobr>     |       | Porta per il server locale (di default una porta casuale)                                       |

---

### serve

Avvia un server Darkie Coder headless per accesso via API. Vedi le [server docs](/docs/server) per l'interfaccia HTTP completa.

```bash
darkie-coder serve
```

Avvia un server HTTP che espone accesso API alle funzionalità di darkie-coder senza la TUI. Imposta `DARKIE_CODER_SERVER_PASSWORD` per abilitare HTTP basic auth (username di default `darkie-coder`).

#### Flag

| Flag                                     | Descrizione                                    |
| ---------------------------------------- | ---------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Porta su cui mettersi in ascolto               |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname su cui mettersi in ascolto            |
| <nobr><code>{"--mdns"}</code></nobr>     | Abilita discovery mDNS                         |
| <nobr><code>{"--cors"}</code></nobr>     | Origin browser addizionali per consentire CORS |

---

### session

Gestisci le sessioni Darkie Coder.

```bash
darkie-coder session [command]
```

---

#### list

Elenca tutte le sessioni Darkie Coder.

```bash
darkie-coder session list
```

##### Flag

| Flag                                      | Breve | Descrizione                          |
| ----------------------------------------- | ----- | ------------------------------------ |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limita alle N sessioni più recenti   |
| <nobr><code>{"--format"}</code></nobr>    |       | Formato output: table o json (table) |

---

### stats

Mostra statistiche di utilizzo token e costo per le sessioni Darkie Coder.

```bash
darkie-coder stats
```

#### Flag

| Flag                                    | Descrizione                                                                           |
| --------------------------------------- | ------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Mostra statistiche per gli ultimi N giorni (all time)                                 |
| <nobr><code>{"--tools"}</code></nobr>   | Numero di strumenti da mostrare (all)                                                 |
| <nobr><code>{"--models"}</code></nobr>  | Mostra breakdown di utilizzo modelli (nascosto di default). Passa un numero per top N |
| <nobr><code>{"--project"}</code></nobr> | Filtra per progetto (tutti i progetti; stringa vuota: progetto corrente)              |

---

### export

Esporta i dati di sessione come JSON.

```bash
darkie-coder export [sessionID]
```

Se non fornisci un ID sessione, ti verrà chiesto di selezionare tra le sessioni disponibili.

---

### import

Importa i dati di sessione da un file JSON o da un URL di condivisione Darkie Coder.

```bash
darkie-coder import <file>
```

Puoi importare da un file locale o da un URL di condivisione Darkie Coder.

```bash
darkie-coder import session.json
darkie-coder import https://opncd.ai/s/abc123
```

---

### web

Avvia un server Darkie Coder headless con interfaccia web.

```bash
darkie-coder web
```

Avvia un server HTTP e apre un browser per accedere a Darkie Coder tramite interfaccia web. Imposta `DARKIE_CODER_SERVER_PASSWORD` per abilitare HTTP basic auth (username di default `darkie-coder`).

#### Flag

| Flag                                     | Descrizione                                    |
| ---------------------------------------- | ---------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Porta su cui mettersi in ascolto               |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname su cui mettersi in ascolto            |
| <nobr><code>{"--mdns"}</code></nobr>     | Abilita discovery mDNS                         |
| <nobr><code>{"--cors"}</code></nobr>     | Origin browser addizionali per consentire CORS |

---

### acp

Avvia un server ACP (Agent Client Protocol).

```bash
darkie-coder acp
```

Questo comando avvia un server ACP che comunica via stdin/stdout usando nd-JSON.

#### Flag

| Flag                                     | Descrizione                         |
| ---------------------------------------- | ----------------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Directory di lavoro                 |
| <nobr><code>{"--port"}</code></nobr>     | Porta su cui mettersi in ascolto    |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname su cui mettersi in ascolto |

---

### uninstall

Disinstalla Darkie Coder e rimuove tutti i file correlati.

```bash
darkie-coder uninstall
```

#### Flag

| Flag                                        | Breve | Descrizione                                  |
| ------------------------------------------- | ----- | -------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Mantieni i file di configurazione            |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Mantieni dati di sessione e snapshot         |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Mostra cosa verrebbe rimosso senza rimuovere |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Salta le richieste di conferma               |

---

### upgrade

Aggiorna darkie-coder all'ultima versione o a una versione specifica.

```bash
darkie-coder upgrade [target]
```

Per aggiornare all'ultima versione:

```bash
darkie-coder upgrade
```

Per aggiornare a una versione specifica:

```bash
darkie-coder upgrade v0.1.48
```

#### Flag

| Flag                                   | Breve | Descrizione                                               |
| -------------------------------------- | ----- | --------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | Metodo di installazione usato: curl, npm, pnpm, bun, brew |

---

## Flag globali

La CLI di darkie-coder accetta i seguenti flag globali.

| Flag                                       | Breve | Descrizione                            |
| ------------------------------------------ | ----- | -------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Mostra l'help                          |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Stampa il numero di versione           |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Stampa i log su stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Livello log (DEBUG, INFO, WARN, ERROR) |

---

## Variabili d'ambiente

Darkie Coder può essere configurato tramite variabili d'ambiente.

| Variabile                             | Tipo    | Descrizione                                                 |
| ------------------------------------- | ------- | ----------------------------------------------------------- |
| `DARKIE_CODER_AUTO_SHARE`                 | boolean | Condivide automaticamente le sessioni                       |
| `DARKIE_CODER_GIT_BASH_PATH`              | string  | Percorso all'eseguibile Git Bash su Windows                 |
| `DARKIE_CODER_CONFIG`                     | string  | Percorso al file di configurazione                          |
| `DARKIE_CODER_TUI_CONFIG`                 | string  | Percorso al file di configurazione TUI                      |
| `DARKIE_CODER_CONFIG_DIR`                 | string  | Percorso alla directory di configurazione                   |
| `DARKIE_CODER_CONFIG_CONTENT`             | string  | Contenuto JSON di config inline                             |
| `DARKIE_CODER_DISABLE_AUTOUPDATE`         | boolean | Disabilita i controlli automatici di aggiornamento          |
| `DARKIE_CODER_DISABLE_PRUNE`              | boolean | Disabilita la potatura dei dati vecchi                      |
| `DARKIE_CODER_DISABLE_TERMINAL_TITLE`     | boolean | Disabilita aggiornamenti automatici del titolo terminale    |
| `DARKIE_CODER_PERMISSION`                 | string  | Config permessi JSON inline                                 |
| `DARKIE_CODER_DISABLE_DEFAULT_PLUGINS`    | boolean | Disabilita i plugin di default                              |
| `DARKIE_CODER_DISABLE_LSP_DOWNLOAD`       | boolean | Disabilita download automatico dei server LSP               |
| `DARKIE_CODER_ENABLE_EXPERIMENTAL_MODELS` | boolean | Abilita modelli sperimentali                                |
| `DARKIE_CODER_DISABLE_AUTOCOMPACT`        | boolean | Disabilita compaction automatica del contesto               |
| `DARKIE_CODER_DISABLE_CLAUDE_CODE`        | boolean | Disabilita lettura da `.claude` (prompt + skill)            |
| `DARKIE_CODER_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disabilita lettura di `~/.claude/CLAUDE.md`                 |
| `DARKIE_CODER_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disabilita caricamento di `.claude/skills`                  |
| `DARKIE_CODER_DISABLE_MODELS_FETCH`       | boolean | Disabilita fetch dei modelli da fonti remote                |
| `DARKIE_CODER_FAKE_VCS`                   | string  | Provider VCS finto per scopi di test                        |
| `DARKIE_CODER_CLIENT`                     | string  | Identificatore client (default `cli`)                       |
| `DARKIE_CODER_ENABLE_EXA`                 | boolean | Abilita gli strumenti di web search Exa                     |
| `DARKIE_CODER_SERVER_PASSWORD`            | string  | Abilita basic auth per `serve`/`web`                        |
| `DARKIE_CODER_SERVER_USERNAME`            | string  | Sovrascrive lo username basic auth (default `darkie-coder`)     |
| `DARKIE_CODER_MODELS_URL`                 | string  | URL personalizzato per recuperare la configurazione modelli |

---

### Sperimentale

Queste variabili d'ambiente abilitano funzionalità sperimentali che potrebbero cambiare o essere rimosse.

| Variabile                                       | Tipo    | Descrizione                                             |
| ----------------------------------------------- | ------- | ------------------------------------------------------- |
| `DARKIE_CODER_EXPERIMENTAL`                         | boolean | Abilita funzionalità sperimentali sotto il flag globale |
| `DARKIE_CODER_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Abilita icon discovery                                  |
| `DARKIE_CODER_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disabilita copy on select nella TUI                     |
| `DARKIE_CODER_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Timeout di default per comandi bash in ms               |
| `DARKIE_CODER_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Massimo token di output per risposte LLM                |
| `DARKIE_CODER_EXPERIMENTAL_FILEWATCHER`             | boolean | Abilita file watcher per l'intera dir                   |
| `DARKIE_CODER_EXPERIMENTAL_OXFMT`                   | boolean | Abilita formatter oxfmt                                 |
| `DARKIE_CODER_EXPERIMENTAL_LSP_TOOL`                | boolean | Abilita strumento LSP sperimentale                      |
| `DARKIE_CODER_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disabilita file watcher                                 |
| `DARKIE_CODER_EXPERIMENTAL_EXA`                     | boolean | Abilita funzionalità Exa sperimentali                   |
| `DARKIE_CODER_EXPERIMENTAL_LSP_TY`                  | boolean | Abilita TY LSP per i file python                        |
| `DARKIE_CODER_EXPERIMENTAL_PLAN_MODE`               | boolean | Abilita plan mode                                       |
| `DARKIE_CODER_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Abilita task subagent in background                     |
| `DARKIE_CODER_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Abilita sistema eventi sperimentale                     |
| `DARKIE_CODER_EXPERIMENTAL_NATIVE_LLM`              | boolean | Abilita percorso nativo di richiesta LLM                |
| `DARKIE_CODER_EXPERIMENTAL_PARALLEL`                | boolean | Abilita esecuzione parallela della ricerca web          |
| `DARKIE_CODER_EXPERIMENTAL_SCOUT`                   | boolean | Abilita subagent Scout                                  |
| `DARKIE_CODER_EXPERIMENTAL_WORKSPACES`              | boolean | Abilita supporto workspace                              |

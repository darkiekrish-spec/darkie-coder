---
title: CLI
description: Параметры и команда darkie-coder CLI.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

CLI darkie-coder по умолчанию запускает [TUI](/docs/tui) при запуске без каких-либо аргументов.

```bash
darkie-coder
```

Но он также принимает команды, описанные на этой странице. Это позволяет вам программно взаимодействовать с darkie-coder.

```bash
darkie-coder run "Explain how closures work in JavaScript"
```

---

### tui

Запустите TUI darkie-coder.

```bash
darkie-coder [project]
```

#### Флаги

| Флаг                                     | Короткий | Описание                                                                      |
| ---------------------------------------- | -------- | ----------------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr> | `-c`     | Продолжить последний сеанс                                                    |
| <nobr><code>{"--session"}</code></nobr>  | `-s`     | Идентификатор сеанса для продолжения                                          |
| <nobr><code>{"--fork"}</code></nobr>     |          | Разветвить сеанс при продолжении (используйте с `--continue` или `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |          | Промпт для использования                                                      |
| <nobr><code>{"--model"}</code></nobr>    | `-m`     | Модель для использования в виде поставщика/модели.                            |
| <nobr><code>{"--agent"}</code></nobr>    |          | Агент для использования                                                       |
| <nobr><code>{"--port"}</code></nobr>     |          | Порт для прослушивания                                                        |
| <nobr><code>{"--hostname"}</code></nobr> |          | Имя хоста для прослушивания                                                   |

---

## Команды

CLI darkie-coder также имеет следующие команды.

---

### agent

Управляйте агентами для darkie-coder.

```bash
darkie-coder agent [command]
```

---

### attach

Подключите терминал к уже работающему внутреннему серверу darkie-coder, запущенному с помощью команд `serve` или `web`.

```bash
darkie-coder attach [url]
```

Это позволяет использовать TUI с удаленным сервером darkie-coder. Например:

```bash
# Start the backend server for web/mobile access
darkie-coder web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
darkie-coder attach http://10.20.30.40:4096
```

#### Флаги

| Флаг                                     | Короткий | Описание                                                                                         |
| ---------------------------------------- | -------- | ------------------------------------------------------------------------------------------------ |
| <nobr><code>{"--dir"}</code></nobr>      |          | Рабочий каталог для запуска TUI                                                                  |
| <nobr><code>{"--continue"}</code></nobr> | `-c`     | Продолжить последний сеанс                                                                       |
| <nobr><code>{"--session"}</code></nobr>  | `-s`     | Идентификатор сеанса для продолжения                                                             |
| <nobr><code>{"--fork"}</code></nobr>     |          | Создать ответвление сеанса при продолжении (используйте с `--continue` или `--session`)          |
| <nobr><code>{"--password"}</code></nobr> | `-p`     | Пароль базовой аутентификации (по умолчанию `DARKIE_CODER_SERVER_PASSWORD`)                          |
| <nobr><code>{"--username"}</code></nobr> | `-u`     | Имя пользователя базовой аутентификации (по умолчанию `DARKIE_CODER_SERVER_USERNAME` или `darkie-coder`) |

---

#### create

Создайте нового агента с пользовательской конфигурацией.

```bash
darkie-coder agent create
```

Эта команда поможет вам создать новый агент с настраиваемой системной подсказкой и настройкой инструмента.

---

#### list

Перечислите всех доступных агентов.

```bash
darkie-coder agent list
```

---

### auth

Команда для управления учетными данными и входом в систему для провайдеров.

```bash
darkie-coder auth [command]
```

---

#### login

darkie-coder использует список провайдеров с [Models.dev](https://models.dev), поэтому вы можете использовать `darkie-coder auth login` для настройки ключей API для любого поставщика, которого вы хотите использовать. Это хранится в `~/.local/share/darkie-coder/auth.json`.

```bash
darkie-coder auth login
```

Когда darkie-coder запускается, он загружает поставщиков из файла учетных данных. И если в ваших средах определены какие-либо ключи или файл `.env` в вашем проекте.

---

#### list

Перечисляет всех проверенных поставщиков, которые хранятся в файле учетных данных.

```bash
darkie-coder auth list
```

Или короткая версия.

```bash
darkie-coder auth ls
```

---

#### logout

Выключает вас из провайдера, удаляя его из файла учетных данных.

```bash
darkie-coder auth logout
```

---

### github

Управляйте агентом GitHub для автоматизации репозитория.

```bash
darkie-coder github [command]
```

---

#### install

Установите агент GitHub в свой репозиторий.

```bash
darkie-coder github install
```

Это настроит необходимый рабочий процесс GitHub Actions и проведет вас через процесс настройки. [Подробнее](/docs/github).

---

#### run

Запустите агент GitHub. Обычно это используется в действиях GitHub.

```bash
darkie-coder github run
```

##### Флаги

| Флаг                                  | Описание                                      |
| ------------------------------------- | --------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | Имитирующее событие GitHub для запуска агента |
| <nobr><code>{"--token"}</code></nobr> | Токен личного доступа GitHub                  |

---

### mcp

Управляйте серверами протокола контекста модели.

```bash
darkie-coder mcp [command]
```

---

#### add

Добавьте сервер MCP в свою конфигурацию.

```bash
darkie-coder mcp add
```

Эта команда поможет вам добавить локальный или удаленный сервер MCP.

---

#### list

Перечислите все настроенные серверы MCP и состояние их подключения.

```bash
darkie-coder mcp list
```

Или используйте короткую версию.

```bash
darkie-coder mcp ls
```

---

#### auth

Аутентификация с помощью сервера MCP с поддержкой OAuth.

```bash
darkie-coder mcp auth [name]
```

Если вы не укажете имя сервера, вам будет предложено выбрать один из доступных серверов с поддержкой OAuth.

Вы также можете перечислить серверы с поддержкой OAuth и их статус аутентификации.

```bash
darkie-coder mcp auth list
```

Или используйте короткую версию.

```bash
darkie-coder mcp auth ls
```

---

#### logout

Удалите учетные данные OAuth для сервера MCP.

```bash
darkie-coder mcp logout [name]
```

---

#### debug

Отладка проблем с подключением OAuth для сервера MCP.

```bash
darkie-coder mcp debug <name>
```

---

### models

Перечислите все доступные модели от настроенных поставщиков.

```bash
darkie-coder models [provider]
```

Эта команда отображает все модели, доступные у настроенных вами поставщиков, в формате `provider/model`.

Это полезно для определения точного названия модели, которое будет использоваться в [вашем config](/docs/config/).

При желании вы можете передать идентификатор поставщика, чтобы фильтровать модели по этому поставщику.

```bash
darkie-coder models anthropic
```

#### Флаги

| Флаг                                    | Описание                                                                          |
| --------------------------------------- | --------------------------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Обновите кеш моделей на сайте models.dev.                                         |
| <nobr><code>{"--verbose"}</code></nobr> | Используйте более подробный вывод модели (включая метаданные, такие как затраты). |

Используйте флаг `--refresh` для обновления списка кэшированных моделей. Это полезно, когда к поставщику добавлены новые модели и вы хотите увидеть их в darkie-coder.

```bash
darkie-coder models --refresh
```

---

### run

Запустите darkie-coder в неинтерактивном режиме, передав приглашение напрямую.

```bash
darkie-coder run [message..]
```

Это полезно для создания сценариев, автоматизации или когда вам нужен быстрый ответ без запуска полного TUI. Например.

```bash "darkie-coder run"
darkie-coder run Explain the use of context in Go
```

Вы также можете подключиться к работающему экземпляру `darkie-coder serve`, чтобы избежать холодной загрузки сервера MCP при каждом запуске:

```bash
# Start a headless server in one terminal
darkie-coder serve

# In another terminal, run commands that attach to it
darkie-coder run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Флаги

| Флаг                                     | Короткий | Описание                                                                                         |
| ---------------------------------------- | -------- | ------------------------------------------------------------------------------------------------ |
| <nobr><code>{"--command"}</code></nobr>  |          | Команда для запуска, используйте сообщение для аргументов                                        |
| <nobr><code>{"--continue"}</code></nobr> | `-c`     | Продолжить последний сеанс                                                                       |
| <nobr><code>{"--session"}</code></nobr>  | `-s`     | Идентификатор сеанса для продолжения                                                             |
| <nobr><code>{"--fork"}</code></nobr>     |          | Разветвить сеанс при продолжении (используйте с `--continue` или `--session`)                    |
| <nobr><code>{"--share"}</code></nobr>    |          | Поделиться сеансом                                                                               |
| <nobr><code>{"--model"}</code></nobr>    | `-m`     | Модель для использования в виде поставщика/модели.                                               |
| <nobr><code>{"--agent"}</code></nobr>    |          | Агент для использования                                                                          |
| <nobr><code>{"--file"}</code></nobr>     | `-f`     | Файл(ы) для прикрепления к сообщению                                                             |
| <nobr><code>{"--format"}</code></nobr>   |          | Формат: по умолчанию (отформатированный) или json (необработанные события JSON).                 |
| <nobr><code>{"--title"}</code></nobr>    |          | Название сеанса (использует усеченное приглашение, если значение не указано)                     |
| <nobr><code>{"--attach"}</code></nobr>   |          | Подключитесь к работающему серверу darkie-coder (например, http://localhost:4096)                    |
| <nobr><code>{"--password"}</code></nobr> | `-p`     | Пароль базовой аутентификации (по умолчанию `DARKIE_CODER_SERVER_PASSWORD`)                          |
| <nobr><code>{"--username"}</code></nobr> | `-u`     | Имя пользователя базовой аутентификации (по умолчанию `DARKIE_CODER_SERVER_USERNAME` или `darkie-coder`) |
| <nobr><code>{"--dir"}</code></nobr>      |          | Каталог для выполнения или путь на удалённом сервере при подключении                             |
| <nobr><code>{"--variant"}</code></nobr>  |          | Вариант модели (уровень рассуждений для провайдера)                                              |
| <nobr><code>{"--thinking"}</code></nobr> |          | Показать блоки размышлений                                                                       |
| <nobr><code>{"--port"}</code></nobr>     |          | Порт локального сервера (по умолчанию случайный порт)                                            |

---

### serve

Запустите автономный сервер darkie-coder для доступа к API. Полный HTTP-интерфейс можно найти в [server docs](/docs/server).

```bash
darkie-coder serve
```

При этом запускается HTTP-сервер, который обеспечивает доступ API к функциям darkie-coder без интерфейса TUI. Установите `DARKIE_CODER_SERVER_PASSWORD`, чтобы включить базовую аутентификацию HTTP (имя пользователя по умолчанию — `darkie-coder`).

#### Флаги

| Флаг                                     | Описание                                                      |
| ---------------------------------------- | ------------------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Порт для прослушивания                                        |
| <nobr><code>{"--hostname"}</code></nobr> | Имя хоста для прослушивания                                   |
| <nobr><code>{"--mdns"}</code></nobr>     | Включить обнаружение mDNS                                     |
| <nobr><code>{"--cors"}</code></nobr>     | Дополнительные источники браузера, позволяющие разрешить CORS |

---

### session

Управляйте сессиями darkie-coder.

```bash
darkie-coder session [command]
```

---

#### list

Перечислите все сеансы darkie-coder.

```bash
darkie-coder session list
```

##### Флаги

| Флаг                                      | Короткий | Описание                                  |
| ----------------------------------------- | -------- | ----------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`     | Ограничить N последних сеансов.           |
| <nobr><code>{"--format"}</code></nobr>    |          | Формат вывода: таблица или json (таблица) |

---

### stats

Покажите статистику использования токенов и затрат для ваших сеансов darkie-coder.

```bash
darkie-coder stats
```

#### Флаги

| Флаг                                    | Описание                                                                                                   |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Показать статистику за последние N дней (все время)                                                        |
| <nobr><code>{"--tools"}</code></nobr>   | Количество инструментов для отображения (все)                                                              |
| <nobr><code>{"--models"}</code></nobr>  | Показать разбивку по использованию модели (по умолчанию скрыто). Передайте номер, чтобы показать верхнюю N |
| <nobr><code>{"--project"}</code></nobr> | Фильтровать по проекту (все проекты, пустая строка: текущий проект)                                        |

---

### export

Экспортируйте данные сеанса в формате JSON.

```bash
darkie-coder export [sessionID]
```

Если вы не укажете идентификатор сеанса, вам будет предложено выбрать один из доступных сеансов.

---

### import

Импортируйте данные сеанса из файла JSON или URL-адреса общего ресурса darkie-coder.

```bash
darkie-coder import <file>
```

Вы можете импортировать из локального файла или URL-адреса общего ресурса darkie-coder.

```bash
darkie-coder import session.json
darkie-coder import https://opncd.ai/s/abc123
```

---

### web

Запустите автономный сервер darkie-coder с веб-интерфейсом.

```bash
darkie-coder web
```

При этом запускается HTTP-сервер и открывается веб-браузер для доступа к darkie-coder через веб-интерфейс. Установите `DARKIE_CODER_SERVER_PASSWORD`, чтобы включить базовую аутентификацию HTTP (имя пользователя по умолчанию — `darkie-coder`).

#### Флаги

| Флаг                                     | Описание                                                      |
| ---------------------------------------- | ------------------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Порт для прослушивания                                        |
| <nobr><code>{"--hostname"}</code></nobr> | Имя хоста для прослушивания                                   |
| <nobr><code>{"--mdns"}</code></nobr>     | Включить обнаружение mDNS                                     |
| <nobr><code>{"--cors"}</code></nobr>     | Дополнительные источники браузера, позволяющие разрешить CORS |

---

### acp

Запустите сервер ACP (агент-клиентский протокол).

```bash
darkie-coder acp
```

Эта команда запускает сервер ACP, который обменивается данными через stdin/stdout с использованием nd-JSON.

#### Флаги

| Флаг                                     | Описание                    |
| ---------------------------------------- | --------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Рабочий каталог             |
| <nobr><code>{"--port"}</code></nobr>     | Порт для прослушивания      |
| <nobr><code>{"--hostname"}</code></nobr> | Имя хоста для прослушивания |

---

### uninstall

Удалите darkie-coder и удалите все связанные файлы.

```bash
darkie-coder uninstall
```

#### Флаги

| Флаг                                        | Короткий | Описание                                   |
| ------------------------------------------- | -------- | ------------------------------------------ |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`     | Сохраняйте файлы конфигурации              |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`     | Храните данные сеанса и снимки             |
| <nobr><code>{"--dry-run"}</code></nobr>     |          | Покажите, что было бы удалено без удаления |
| <nobr><code>{"--force"}</code></nobr>       | `-f`     | Пропустить запросы подтверждения           |

---

### upgrade

Обновляет darkie-coder до последней версии или определенной версии.

```bash
darkie-coder upgrade [target]
```

Чтобы обновиться до последней версии.

```bash
darkie-coder upgrade
```

Для обновления до определенной версии.

```bash
darkie-coder upgrade v0.1.48
```

#### Флаги

| Флаг                                   | Короткий | Описание                                                  |
| -------------------------------------- | -------- | --------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`     | Используемый метод установки: local, npm, pnpm, bun, brew |

---

## Глобальные флаги

CLI darkie-coder принимает следующие глобальные флаги.

| Флаг                                       | Короткий | Описание                                   |
| ------------------------------------------ | -------- | ------------------------------------------ |
| <nobr><code>{"--help"}</code></nobr>       | `-h`     | Отобразить справку                         |
| <nobr><code>{"--version"}</code></nobr>    | `-v`     | Распечатать номер версии                   |
| <nobr><code>{"--print-logs"}</code></nobr> |          | Печать журналов в stderr                   |
| <nobr><code>{"--log-level"}</code></nobr>  |          | Уровень журнала (DEBUG, INFO, WARN, ERROR) |

---

## Переменные среды

darkie-coder можно настроить с помощью переменных среды.

| Переменная                            | Тип                 | Описание                                                                         |
| ------------------------------------- | ------------------- | -------------------------------------------------------------------------------- |
| `DARKIE_CODER_AUTO_SHARE`                 | логическое значение | Автоматически делиться сеансами                                                  |
| `DARKIE_CODER_GIT_BASH_PATH`              | строка              | Путь к исполняемому файлу Git Bash в Windows                                     |
| `DARKIE_CODER_CONFIG`                     | строка              | Путь к файлу конфигурации                                                        |
| `DARKIE_CODER_TUI_CONFIG`                 | строка              | Путь к файлу конфигурации TUI                                                    |
| `DARKIE_CODER_CONFIG_DIR`                 | строка              | Путь к каталогу конфигурации                                                     |
| `DARKIE_CODER_CONFIG_CONTENT`             | строка              | Встроенное содержимое конфигурации json                                          |
| `DARKIE_CODER_DISABLE_AUTOUPDATE`         | логическое значение | Отключить автоматическую проверку обновлений                                     |
| `DARKIE_CODER_DISABLE_PRUNE`              | логическое значение | Отключить удаление старых данных                                                 |
| `DARKIE_CODER_DISABLE_TERMINAL_TITLE`     | логическое значение | Отключить автоматическое обновление заголовка терминала                          |
| `DARKIE_CODER_PERMISSION`                 | строка              | Встроенная конфигурация разрешений json                                          |
| `DARKIE_CODER_DISABLE_DEFAULT_PLUGINS`    | логическое значение | Отключить плагины по умолчанию                                                   |
| `DARKIE_CODER_DISABLE_LSP_DOWNLOAD`       | логическое значение | Отключить автоматическую загрузку LSP-сервера                                    |
| `DARKIE_CODER_ENABLE_EXPERIMENTAL_MODELS` | логическое значение | Включить экспериментальные модели                                                |
| `DARKIE_CODER_DISABLE_AUTOCOMPACT`        | логическое значение | Отключить автоматическое сжатие контекста                                        |
| `DARKIE_CODER_DISABLE_CLAUDE_CODE`        | логическое значение | Отключить чтение из `.claude` (подсказка + навыки)                               |
| `DARKIE_CODER_DISABLE_CLAUDE_CODE_PROMPT` | логическое значение | Отключить чтение `~/.claude/CLAUDE.md`                                           |
| `DARKIE_CODER_DISABLE_CLAUDE_CODE_SKILLS` | логическое значение | Отключить загрузку `.claude/skills`                                              |
| `DARKIE_CODER_DISABLE_MODELS_FETCH`       | логическое значение | Отключить получение моделей из удаленных источников                              |
| `DARKIE_CODER_FAKE_VCS`                   | строка              | Поддельный поставщик VCS для целей тестирования                                  |
| `DARKIE_CODER_CLIENT`                     | строка              | Идентификатор клиента (по умолчанию `cli`)                                       |
| `DARKIE_CODER_ENABLE_EXA`                 | логическое значение | Включить инструменты веб-поиска Exa                                              |
| `DARKIE_CODER_SERVER_PASSWORD`            | строка              | Включить базовую аутентификацию для `serve`/`web`                                |
| `DARKIE_CODER_SERVER_USERNAME`            | строка              | Переопределить имя пользователя базовой аутентификации (по умолчанию `darkie-coder`) |
| `DARKIE_CODER_MODELS_URL`                 | строка              | Пользовательский URL-адрес для получения конфигурации модели                     |

---

### Экспериментальные функции

Эти переменные среды позволяют использовать экспериментальные функции, которые могут быть изменены или удалены.

| Переменная                                      | Тип                 | Описание                                               |
| ----------------------------------------------- | ------------------- | ------------------------------------------------------ |
| `DARKIE_CODER_EXPERIMENTAL`                         | логическое значение | Включить экспериментальные функции под общим флагом    |
| `DARKIE_CODER_EXPERIMENTAL_ICON_DISCOVERY`          | логическое значение | Включить обнаружение значков                           |
| `DARKIE_CODER_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | логическое значение | Отключить копирование при выборе в TUI                 |
| `DARKIE_CODER_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | число               | Таймаут по умолчанию для команд bash в мс              |
| `DARKIE_CODER_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | число               | Максимальное количество токенов вывода для ответов LLM |
| `DARKIE_CODER_EXPERIMENTAL_FILEWATCHER`             | логическое значение | Включить просмотр файлов для всего каталога            |
| `DARKIE_CODER_EXPERIMENTAL_OXFMT`                   | логическое значение | Включить форматтер oxfmt                               |
| `DARKIE_CODER_EXPERIMENTAL_LSP_TOOL`                | логическое значение | Включить экспериментальный инструмент LSP              |
| `DARKIE_CODER_EXPERIMENTAL_DISABLE_FILEWATCHER`     | логическое значение | Отключить просмотрщик файлов                           |
| `DARKIE_CODER_EXPERIMENTAL_EXA`                     | логическое значение | Включить экспериментальные функции Exa                 |
| `DARKIE_CODER_EXPERIMENTAL_LSP_TY`                  | логическое значение | Включить TY LSP для файлов python                      |
| `DARKIE_CODER_EXPERIMENTAL_PLAN_MODE`               | логическое значение | Включить режим плана                                   |
| `DARKIE_CODER_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | логическое значение | Включить фоновые задачи субагентов                     |
| `DARKIE_CODER_EXPERIMENTAL_EVENT_SYSTEM`            | логическое значение | Включить экспериментальную систему событий             |
| `DARKIE_CODER_EXPERIMENTAL_NATIVE_LLM`              | логическое значение | Включить нативный путь запросов LLM                    |
| `DARKIE_CODER_EXPERIMENTAL_PARALLEL`                | логическое значение | Включить параллельное выполнение веб-поиска            |
| `DARKIE_CODER_EXPERIMENTAL_SCOUT`                   | логическое значение | Включить субагент Scout                                |
| `DARKIE_CODER_EXPERIMENTAL_WORKSPACES`              | логическое значение | Включить поддержку рабочих областей                    |

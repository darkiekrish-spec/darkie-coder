---
title: CLI
description: Darkie Coder CLI muligheder og kommandoer.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

Darkie Coder CLI starter som standard [TUI](/docs/tui), når den kører uden nogen argumenter.

```bash
darkie-coder
```

Men den accepterer også kommandoer som dokumenteret på denne side. Dette giver dig mulighed for at interagere med Darkie Coder programmatisk.

```bash
darkie-coder run "Explain how closures work in JavaScript"
```

---

### tui

Start Darkie Coder terminalbrugergrænsefladen.

```bash
darkie-coder [project]
```

#### Flag

| Flag                                     | Kort | Beskrivelse                                                                  |
| ---------------------------------------- | ---- | ---------------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Fortsæt sidste session                                                       |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Sessions-id for at fortsætte                                                 |
| <nobr><code>{"--fork"}</code></nobr>     |      | Forgren sessionen ved fortsættelse (brug med `--continue` eller `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |      | Spørg om at bruge                                                            |
| <nobr><code>{"--model"}</code></nobr>    | `-m` | Model til brug i form af provider/model                                      |
| <nobr><code>{"--agent"}</code></nobr>    |      | Agent hos bruge                                                              |
| <nobr><code>{"--port"}</code></nobr>     |      | Port at lytte på                                                             |
| <nobr><code>{"--hostname"}</code></nobr> |      | Værtsnavn at lytte på                                                        |

---

## Kommandoer

Darkie Coder CLI har også følgende kommandoer.

---

### agent

Administrer agenter i Darkie Coder.

```bash
darkie-coder agent [command]
```

---

### attach

Tilslut en terminal til en allerede kørende Darkie Coder backend-server startet via `serve` eller `web` kommandoer.

```bash
darkie-coder attach [url]
```

Dette gør det muligt at bruge TUI med en ekstern Darkie Coder backend. For eksempel:

```bash
# Start the backend server for web/mobile access
darkie-coder web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
darkie-coder attach http://10.20.30.40:4096
```

#### Flag

| Flag                                     | Kort | Beskrivelse                                                                       |
| ---------------------------------------- | ---- | --------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |      | Arbejdsmappe til at starte TUI i                                                  |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Fortsæt den seneste session                                                       |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Sessions-id for at fortsætte                                                      |
| <nobr><code>{"--fork"}</code></nobr>     |      | Forgren sessionen ved fortsættelse (brug med `--continue` eller `--session`)      |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Adgangskode til basic auth (standard: `DARKIE_CODER_SERVER_PASSWORD`)                 |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Brugernavn til basic auth (standard: `DARKIE_CODER_SERVER_USERNAME` eller `darkie-coder`) |

---

#### create

Opret en ny agent med brugerdefineret konfiguration.

```bash
darkie-coder agent create
```

Denne kommando vil guide dig gennem oprettelse af en ny agent med en brugerdefineret systemprompt og værktøjskonfiguration.

---

#### list

Liste over alle tilgængelige agenter.

```bash
darkie-coder agent list
```

---

### auth

Kommando til at administrere legitimationsoplysninger og login for udbydere.

```bash
darkie-coder auth [command]
```

---

#### login

Darkie Coder drives af udbyderlisten på [Models.dev](https://models.dev), så du kan bruge `darkie-coder auth login` til at konfigurere API-nøgler for enhver udbyder, du vil bruge. Dette er gemt i `~/.local/share/darkie-coder/auth.json`.

```bash
darkie-coder auth login
```

Når Darkie Coder starter op, indlæser den udbyderne fra legitimationsfilen. Og hvis der er nogen nøgler defineret i dine miljøer eller en `.env` fil i dit projekt.

---

#### list

Viser alle de godkendte udbydere som gemt i legitimationsfilen.

```bash
darkie-coder auth list
```

Eller den korte version.

```bash
darkie-coder auth ls
```

---

#### logout

Logger dig ud af en udbyder ved at rydde den fra legitimationsfilen.

```bash
darkie-coder auth logout
```

---

### github

Administrator GitHub-agenten til lagerautomatisering.

```bash
darkie-coder github [command]
```

---

#### install

Installatør GitHub-agenten i dit lager.

```bash
darkie-coder github install
```

Dette opsætter det nødvendige GitHub Actions workflow og guider dig gennem konfigurationsprocessen. [Learn more](/docs/github).

---

#### run

Kør GitHub-agenten. Dette bruges typisk i GitHub Actions.

```bash
darkie-coder github run
```

##### Flag

| Flag                                  | Beskrivelse                                    |
| ------------------------------------- | ---------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub mock begivenhed for at køre agenten for |
| <nobr><code>{"--token"}</code></nobr> | GitHub personlig adgangstoken                  |

---

### mcp

Administrator Model Context Protocol-servere.

```bash
darkie-coder mcp [command]
```

---

#### add

Tilføj en MCP-server til din konfiguration.

```bash
darkie-coder mcp add
```

Denne kommando vil guide dig gennem tilføjelse af enten en lokal eller ekstern MCP-server.

---

#### list

Liste over alle konfigurerede MCP-servere og deres forbindelsesstatus.

```bash
darkie-coder mcp list
```

Eller brug den korte version.

```bash
darkie-coder mcp ls
```

---

#### auth

Godkendt med en OAuth-aktiveret MCP-server.

```bash
darkie-coder mcp auth [name]
```

Hvis du ikke angiver et servernavn, bliver du bedt om at vælge blandt tilgængelige OAuth-kompatible servere.

Du kan også angive OAuth-kompatible servere og deres godkendelsesstatus.

```bash
darkie-coder mcp auth list
```

Eller brug den korte version.

```bash
darkie-coder mcp auth ls
```

---

#### logout

Fjern OAuth-legitimationsoplysninger for en MCP-server.

```bash
darkie-coder mcp logout [name]
```

---

#### debug

Fejlfinding af OAuth-forbindelsesproblemer for en MCP-server.

```bash
darkie-coder mcp debug <name>
```

---

### models

Liste over alle tilgængelige modeller fra konfigurerede udbydere.

```bash
darkie-coder models [provider]
```

Denne kommando viser alle tilgængelige modeller på tværs af dine konfigurerede udbydere i formatet `provider/model`.

Dette er nyttigt til at finde ud af det nøjagtige modelnavn, der skal bruges i [your config](/docs/config/).

Du kan eventuelt videregive et udbyder-id for at filtrere modeller efter den udbyder.

```bash
darkie-coder models anthropic
```

#### Flag

| Flag                                    | Beskrivelse                                                             |
| --------------------------------------- | ----------------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Opdater modelcachen fra models.dev                                      |
| <nobr><code>{"--verbose"}</code></nobr> | Brug mere detaljeret modeloutput (inkluderer metadata som omkostninger) |

Brug flaget `--refresh` til at opdatere den cachelagrede modelliste. Dette er nyttigt, når nye modeller er blevet tilføjet til en udbyder, og du vil se dem i Darkie Coder.

```bash
darkie-coder models --refresh
```

---

### run

Kør darkie-coder i ikke-interaktiv tilstand ved at sende en prompt direkte.

```bash
darkie-coder run [message..]
```

Dette er nyttigt til scripting, automatisering, eller når du vil have et hurtigt svar uden at starte hele TUI. F.eks.

```bash "darkie-coder run"
darkie-coder run Explain the use of context in Go
```

Du kan også vedhæfte til en kørende `darkie-coder serve`-instans for at undgå MCP serverens kolde opstartstider ved hver kørsel:

```bash
# Start a headless server in one terminal
darkie-coder serve

# In another terminal, run commands that attach to it
darkie-coder run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flag

| Flag                                     | Kort | Beskrivelse                                                                         |
| ---------------------------------------- | ---- | ----------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |      | Kommandoen til at køre, brug besked til args                                        |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Fortsæt sidste session                                                              |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Sessions-id for at fortsætte                                                        |
| <nobr><code>{"--fork"}</code></nobr>     |      | Forgren sessionen ved fortsættelse (brug med `--continue` eller `--session`)        |
| <nobr><code>{"--share"}</code></nobr>    |      | Del sessionen                                                                       |
| <nobr><code>{"--model"}</code></nobr>    | `-m` | Model til brug i form af provider/model                                             |
| <nobr><code>{"--agent"}</code></nobr>    |      | Agent til brug                                                                      |
| <nobr><code>{"--file"}</code></nobr>     | `-f` | Fil(er), der skal vedhæftes til meddelelsen                                         |
| <nobr><code>{"--format"}</code></nobr>   |      | Format: standard (formateret) eller json (rå JSON hændelser)                        |
| <nobr><code>{"--title"}</code></nobr>    |      | Titel for sessionen (bruger trunkeret prompt, hvis der ikke er angivet nogen værdi) |
| <nobr><code>{"--attach"}</code></nobr>   |      | Tilslut til en kørende darkie-coder-server (f.eks. http://localhost:4096)               |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Adgangskode til basic auth (standard: `DARKIE_CODER_SERVER_PASSWORD`)                   |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Brugernavn til basic auth (standard: `DARKIE_CODER_SERVER_USERNAME` eller `darkie-coder`)   |
| <nobr><code>{"--dir"}</code></nobr>      |      | Mappe at køre i, eller sti på fjernserveren ved tilkobling                          |
| <nobr><code>{"--variant"}</code></nobr>  |      | Modelvariant (udbyder-specifik ræsonneringsindsats)                                 |
| <nobr><code>{"--thinking"}</code></nobr> |      | Vis tænkeblokke                                                                     |
| <nobr><code>{"--port"}</code></nobr>     |      | Port til den lokale server (standard til vilkårlig port)                            |

---

### serve

Start en hovedløs Darkie Coder-server til API-adgang. Tjek [server docs](/docs/server) for den fulde HTTP-grænseflade.

```bash
darkie-coder serve
```

Dette starter en HTTP-server, der giver API-adgang til darkie-coder-funktionalitet uden TUI-grænsefladen. Indstil `DARKIE_CODER_SERVER_PASSWORD` for at aktivere HTTP grundlæggende godkendelse (brugernavn er standard til `darkie-coder`).

#### Flag

| Flag                                     | Beskrivelse                                      |
| ---------------------------------------- | ------------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>     | Port at lytte på                                 |
| <nobr><code>{"--hostname"}</code></nobr> | Værtsnavn at lytte på                            |
| <nobr><code>{"--mdns"}</code></nobr>     | Aktiver mDNS-opdagelse                           |
| <nobr><code>{"--cors"}</code></nobr>     | Yderligere browseroprindelse til at tillade CORS |

---

### session

Administrator Darkie Coder sessionsholder.

```bash
darkie-coder session [command]
```

---

#### list

Liste over alle Darkie Coder sessioner.

```bash
darkie-coder session list
```

##### Flag

| Flag                                      | Kort | Beskrivelse                            |
| ----------------------------------------- | ---- | -------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n` | Begræns til N seneste sessioner        |
| <nobr><code>{"--format"}</code></nobr>    |      | Outputformat: tabel eller json (tabel) |

---

### stats

Vis tokenbrug og omkostningsstatistikker for dine Darkie Coder-sessioner.

```bash
darkie-coder stats
```

#### Flag

| Flag                                    | Beskrivelse                                                                 |
| --------------------------------------- | --------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Vis statistik for de sidste N dage (hele tiden)                             |
| <nobr><code>{"--tools"}</code></nobr>   | Antal værktøjer, der skal vises (alle)                                      |
| <nobr><code>{"--models"}</code></nobr>  | Vis modelbrugsopdeling (skjult som standard). Send et tal for at vise top N |
| <nobr><code>{"--project"}</code></nobr> | Filtre efter projekt (alle projekter, tom streng: nuværende projekt)        |

---

### export

Eksporter sessionsdata som JSON.

```bash
darkie-coder export [sessionID]
```

Hvis du ikke angiver et sessions-id, bliver du bedt om at vælge mellem tilgængelige sessioner.

---

### import

Importer sessionsdata fra en JSON fil eller Darkie Coder del URL.

```bash
darkie-coder import <file>
```

Du kan importere fra en lokal fil eller en Darkie Coder share URL.

```bash
darkie-coder import session.json
darkie-coder import https://opncd.ai/s/abc123
```

---

### web

Start en hovedløs Darkie Coder-server med en webgrænseflade.

```bash
darkie-coder web
```

Dette starter en HTTP-server og åbner en webbrowser for at få adgang til Darkie Coder via en webgrænseflade. Indstil `DARKIE_CODER_SERVER_PASSWORD` for at aktivere HTTP grundlæggende godkendelse (brugernavn er standard til `darkie-coder`).

#### Flag

| Flag                                     | Beskrivelse                                      |
| ---------------------------------------- | ------------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>     | Port at lytte på                                 |
| <nobr><code>{"--hostname"}</code></nobr> | Værtsnavn at lytte på                            |
| <nobr><code>{"--mdns"}</code></nobr>     | Aktiver mDNS-opdagelse                           |
| <nobr><code>{"--cors"}</code></nobr>     | Yderligere browseroprindelse til at tillade CORS |

---

### acp

Start en ACP-server (Agent Client Protocol).

```bash
darkie-coder acp
```

Denne kommando starter en ACP-server, der kommunikerer via stdin/stdout ved hjælp af nd-JSON.

#### Flag

| Flag                                     | Beskrivelse           |
| ---------------------------------------- | --------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Arbejdsmappe          |
| <nobr><code>{"--port"}</code></nobr>     | Port at lytte på      |
| <nobr><code>{"--hostname"}</code></nobr> | Værtsnavn at lytte på |

---

### uninstall

Afinstaller Darkie Coder og fjern alle relaterede filer.

```bash
darkie-coder uninstall
```

#### Flag

| Flag                                        | Kort | Beskrivelse                                      |
| ------------------------------------------- | ---- | ------------------------------------------------ |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c` | Se konfigurationsfiler                           |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d` | Gem sessionsdata og snapshots                    |
| <nobr><code>{"--dry-run"}</code></nobr>     |      | Vis, hvad der ville blive fjernet uden at fjerne |
| <nobr><code>{"--force"}</code></nobr>       | `-f` | Spring bekræftelsesspørgsmål over                |

---

### upgrade

Opdaterer Darkie Coder til den nyeste version eller en specifik version.

```bash
darkie-coder upgrade [target]
```

For at opgradere til den nyeste version.

```bash
darkie-coder upgrade
```

For at opgradere til en bestemt version.

```bash
darkie-coder upgrade v0.1.48
```

#### upgrade

| Flag                                   | Kort | Beskrivelse                                                      |
| -------------------------------------- | ---- | ---------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m` | Installationsmetoden, der blev brugt; curl, npm, pnpm, bun, brew |

---

## Globale flag

darkie-coder CLI tager følgende globale flag.

| Flag                                       | Kort | Beskrivelse                          |
| ------------------------------------------ | ---- | ------------------------------------ |
| <nobr><code>{"--help"}</code></nobr>       | `-h` | Vis hjælp                            |
| <nobr><code>{"--version"}</code></nobr>    | `-v` | Udskriftsversionsnummer              |
| <nobr><code>{"--print-logs"}</code></nobr> |      | Udskriv logfiler til stderr          |
| <nobr><code>{"--log-level"}</code></nobr>  |      | Logniveau (DEBUG, INFO, WARN, ERROR) |

---

## Miljøvariabler

Darkie Coder kan konfigureres ved hjælp af miljøvariabler.

| Variabel                              | Skriv   | Beskrivelse                                                           |
| ------------------------------------- | ------- | --------------------------------------------------------------------- |
| `DARKIE_CODER_AUTO_SHARE`                 | boolean | Del automatisk session                                                |
| `DARKIE_CODER_GIT_BASH_PATH`              | string  | Sti til Git Bash eksekverbar på Windows                               |
| `DARKIE_CODER_CONFIG`                     | string  | Sti til konfigurationsfil                                             |
| `DARKIE_CODER_TUI_CONFIG`                 | string  | Sti til TUI-konfigurationsfil                                         |
| `DARKIE_CODER_CONFIG_DIR`                 | string  | Sti til konfigurationsmappe                                           |
| `DARKIE_CODER_CONFIG_CONTENT`             | string  | Indbygget json-konfigurationsindhold                                  |
| `DARKIE_CODER_DISABLE_AUTOUPDATE`         | boolean | Deaktiver automatisk opdateringskontrol                               |
| `DARKIE_CODER_DISABLE_PRUNE`              | boolean | Deaktiver beskæring af gamle data                                     |
| `DARKIE_CODER_DISABLE_TERMINAL_TITLE`     | boolean | Deaktiver automatisk opdatering af terminaltitel                      |
| `DARKIE_CODER_PERMISSION`                 | string  | Indbygget json-tilladelseskonfiguration                               |
| `DARKIE_CODER_DISABLE_DEFAULT_PLUGINS`    | boolean | Deaktiver standard plugins                                            |
| `DARKIE_CODER_DISABLE_LSP_DOWNLOAD`       | boolean | Deaktiver automatisk LSP-serverdownloads                              |
| `DARKIE_CODER_ENABLE_EXPERIMENTAL_MODELS` | boolean | Aktive eksperimentelle modeller                                       |
| `DARKIE_CODER_DISABLE_AUTOCOMPACT`        | boolean | Deaktiver automatisk kontekstkomprimering                             |
| `DARKIE_CODER_DISABLE_CLAUDE_CODE`        | boolean | Deaktiver læsning fra `.claude` (prompt + færdigheder)                |
| `DARKIE_CODER_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Deaktiver læsning `~/.claude/CLAUDE.md`                               |
| `DARKIE_CODER_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Deaktiver indlæsning af `.claude/skills`                              |
| `DARKIE_CODER_DISABLE_MODELS_FETCH`       | boolean | Deaktivering af modeller fra eksterne kilder                          |
| `DARKIE_CODER_FAKE_VCS`                   | string  | Falsk VCS-udbyder til testformål                                      |
| `DARKIE_CODER_CLIENT`                     | string  | Klient-id (standard til `cli`)                                        |
| `DARKIE_CODER_ENABLE_EXA`                 | boolean | Aktiver Exa-websøgeværktøjer                                          |
| `DARKIE_CODER_SERVER_PASSWORD`            | string  | Aktiver grundlæggende godkendelse for `serve`/`web`                   |
| `DARKIE_CODER_SERVER_USERNAME`            | string  | Tilsidesæt grundlæggende godkendelsesbrugernavn (standard `darkie-coder`) |
| `DARKIE_CODER_MODELS_URL`                 | string  | Brugerdefineret URL til hentning af modelkonfiguration                |

---

### Eksperimentel

Disse miljøvariabler muliggør eksperimentelle funktioner, der kan ændres eller fjernes.

| Variabel                                        | Skriv   | Beskrivelse                                          |
| ----------------------------------------------- | ------- | ---------------------------------------------------- |
| `DARKIE_CODER_EXPERIMENTAL`                         | boolean | Aktiver eksperimentelle funktioner under samleflaget |
| `DARKIE_CODER_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Aktiver ikonopdagelse                                |
| `DARKIE_CODER_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Deaktiver kopi ved valg i TUI                        |
| `DARKIE_CODER_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | nummer  | Standard timeout for bash-kommandoer i ms            |
| `DARKIE_CODER_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | nummer  | Maks. output-tokens for LLM-svar                     |
| `DARKIE_CODER_EXPERIMENTAL_FILEWATCHER`             | boolean | Aktiver filovervågning for hele dir                  |
| `DARKIE_CODER_EXPERIMENTAL_OXFMT`                   | boolean | Aktiver oxfmt formatter                              |
| `DARKIE_CODER_EXPERIMENTAL_LSP_TOOL`                | boolean | Aktive eksperimenter LSP værktøj                     |
| `DARKIE_CODER_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Deaktiver filovervågning                             |
| `DARKIE_CODER_EXPERIMENTAL_EXA`                     | boolean | Aktive eksperimenter Exa-funktioner                  |
| `DARKIE_CODER_EXPERIMENTAL_LSP_TY`                  | boolean | Aktiver TY LSP for python-filer                      |
| `DARKIE_CODER_EXPERIMENTAL_PLAN_MODE`               | boolean | Aktiver plantilstand                                 |
| `DARKIE_CODER_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Aktiver baggrundsopgaver for subagenter              |
| `DARKIE_CODER_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Aktiver eksperimentelt hændelsessystem               |
| `DARKIE_CODER_EXPERIMENTAL_NATIVE_LLM`              | boolean | Aktiver native LLM-anmodningssti                     |
| `DARKIE_CODER_EXPERIMENTAL_PARALLEL`                | boolean | Aktiver parallel udførelse af websøgning             |
| `DARKIE_CODER_EXPERIMENTAL_SCOUT`                   | boolean | Aktiver Scout-subagent                               |
| `DARKIE_CODER_EXPERIMENTAL_WORKSPACES`              | boolean | Aktiver workspace-understøttelse                     |
